---
phase: 05-watch-management-write-path
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/lib/copy.ts
  - dashboard/app/globals.css
  - dashboard/app/watches/unlock-prompt.tsx
autonomous: true
requirements: [MGMT-06]

must_haves:
  truths:
    - "Every user-visible string this phase introduces lives in one file and matches the UI-SPEC copywriting contract verbatim"
    - "An unauthenticated visitor sees an inline 'Unlock to manage watches' form, not a redirect to a separate login page"
    - "Submitting the correct passphrase unlocks management controls without a full page reload; submitting a wrong one shows an inline error and keeps the form"
    - "The new UI introduces no new color, no fifth font size, and no third font weight"
  artifacts:
    - path: "dashboard/lib/copy.ts"
      provides: "COPY keys for every string in 05-UI-SPEC.md's Copywriting Contract"
      contains: "unlockHeading"
    - path: "dashboard/app/globals.css"
      provides: "CSS classes for buttons, chips, dialog, typeahead, preview, toast — using only existing tokens"
      contains: "--space-"
    - path: "dashboard/app/watches/unlock-prompt.tsx"
      provides: "the dashboard's first 'use client' component: inline passphrase form (D-02)"
      exports: ["UnlockPrompt"]
  key_links:
    - from: "dashboard/app/watches/unlock-prompt.tsx"
      to: "/api/session"
      via: "fetch POST { passphrase }"
      pattern: "fetch\\('/api/session'"
    - from: "dashboard/app/watches/unlock-prompt.tsx"
      to: "dashboard/lib/copy.ts"
      via: "COPY.unlock* keys, no inline strings"
      pattern: "COPY\\."
---

<objective>
Lay the presentation foundation for the entire write-path UI: every new string as a `COPY` key, every new CSS class built from the existing design tokens, and the dashboard's first-ever `'use client'` component — the inline unlock form.

Purpose: Plans 05-05 and 05-06 build five more components. If they each invent strings and classes ad hoc, the UI-SPEC contract silently rots. Landing the whole vocabulary first means those plans only write behavior.
Output: `dashboard/lib/copy.ts` (extended), `dashboard/app/globals.css` (extended), `dashboard/app/watches/unlock-prompt.tsx` (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-watch-management-write-path/05-UI-SPEC.md
@.planning/phases/05-watch-management-write-path/05-PATTERNS.md

<interfaces>
<!-- Existing design tokens in dashboard/app/globals.css :root — USE THESE, INVENT NOTHING. -->
```css
--space-xs: 4px;  --space-sm: 8px;  --space-md: 16px;
--space-lg: 24px; --space-xl: 32px; --space-2xl: 48px; --space-3xl: 64px;

--font-sans: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
--text-meta: 14px; --text-body: 16px; --text-heading: 20px; --text-title: 28px;
--weight-regular: 400; --weight-semibold: 600;
--leading-tight: 1.2; --leading-normal: 1.5;

--color-bg: #FFFFFF;        --color-surface: #F1F3F5;   --color-border: #E2E5E9;
--color-accent: #2563EB;    --color-destructive: #DC2626;
--color-text: #111827;      --color-meta: #6B7280;
--color-match: #16A34A;     --color-no-match: #6B7280;  --color-failed: #DC2626;
```

Existing classes already in globals.css (reuse, do not redefine): `.page`, `.page-title`,
`.freshness`, `.sections`, `.section`, `.section-heading`, `.rows`, `.row`, `.row-main`,
`.row-meta`, `.badge`, `.badge--match`, `.badge--no-match`, `.badge--failed`, `.badge--unknown`,
`.failure-reason`, `.booking-link`, `.empty-heading`, `.empty-body`, `.error`, `.error-heading`,
`.error-body`.

Existing `dashboard/lib/copy.ts` shape (append to the same `COPY` object literal):
```typescript
export const COPY = {
  pageTitle: 'Campground Crawler — Status',
  /* ...existing keys... */
} as const;
```

The `/api/session` contract (built in plan 05-03, code against it — do not implement it here):
- `POST /api/session` body `{ passphrase: string }` → `200 { ok: true }` on match, `401 { ok: false }` on mismatch, `400 { ok: false }` on a malformed body
- `DELETE /api/session` → `200 { ok: true }`, clears the cookie
- The cookie is `httpOnly`, so client JavaScript **cannot** read it. Lock state must be passed
  down from the Server Component as a prop, never inferred from `document.cookie`.

Files under `dashboard/app/**` use the `@/` path alias (`@/lib/copy`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add every new UI string to dashboard/lib/copy.ts</name>

  <files>dashboard/lib/copy.ts</files>

  <read_first>
    - `dashboard/lib/copy.ts` — the file being modified; note its header ("Changing a value here is a UI-SPEC change — update the spec first") and the `as const` object-literal shape
    - `.planning/phases/05-watch-management-write-path/05-UI-SPEC.md` — the Copywriting Contract table (lines ~92-120) is the verbatim source for every value below
  </read_first>

  <action>
Append these keys to the existing `COPY` object in `dashboard/lib/copy.ts`, preserving the file's
comment style. Values are **verbatim from 05-UI-SPEC.md's Copywriting Contract** — do not reword,
do not "improve", do not change punctuation (note the ellipsis character `…`, the em-dashes `—`,
and the curly apostrophes `’` in "didn't"/"can't"/"Couldn't" — copy them exactly as written below):

```typescript
  // --- Phase 5: watch management write path (05-UI-SPEC.md Copywriting Contract) ---
  sectionManageWatches: 'Manage Watches',

  addWatch: '+ Add Watch',
  saveWatch: 'Save Watch',
  discardChanges: 'Discard Changes',

  toggleFacility: 'Single Campground',
  toggleArea: 'Recreation Area(s)',

  areaSearchPlaceholder: 'Search Recreation Areas by name…',
  /** `{query}` is replaced at render time with the user's current input. */
  areaSearchNoResults: 'No Recreation Areas found for "{query}"',
  areaSearchMinChars: 'Type at least 2 characters to search',
  /** `{area}` is replaced with the chip's area name. */
  areaChipRemoveLabel: 'Remove {area}',

  previewHeading: 'Resolves to these campgrounds',
  previewLoading: 'Checking which campgrounds match…',
  previewEmpty: 'Add a Recreation Area above to see which campgrounds this watch will check.',
  /** `{kept}`, `{requested}`, `{cap}` are replaced from the /api/ridb/preview response. */
  previewTruncated: 'Showing {kept} of {requested} campgrounds — this watch is at the {cap}-campground limit.',
  /** Appended to a campground name when facilityType === 'group'. Standard campgrounds get no
   *  tag. Matches derive-active-matches.ts's existing [GROUP] suffix convention exactly. */
  groupTag: '[GROUP]',

  unlockHeading: 'Unlock to manage watches',
  unlockBody: 'Enter the shared passphrase to create, edit, or delete watches. Read-only viewing never requires this.',
  unlockPlaceholder: 'Passphrase',
  unlockSubmit: 'Unlock Watches',
  unlockError: 'That passphrase didn’t match. Try again.',

  /** `{reason}` is replaced with the API's error string, or dropped entirely when unknown. */
  saveFailed: 'Couldn’t save this watch. {reason} — nothing was changed. Try again.',
  areaSearchFailed: 'Couldn’t search Recreation Areas right now. Try again in a moment.',

  deleteConfirm: 'Delete this watch? This can’t be undone.',
  deleteConfirmYes: 'Delete',
  deleteConfirmNo: 'Keep Watch',

  savedToast: 'Saved — live within ~5 min',
  deletedToast: 'Deleted — live within ~5 min',

  /** `{watch}` is replaced with the watch id. */
  editWatchLabel: 'Edit watch: {watch}',
  deleteWatchLabel: 'Delete watch: {watch}',

  modalHeadingCreate: 'Create Watch',
  modalHeadingEdit: 'Edit Watch',
```

Note: `sectionManageWatches`, `modalHeadingCreate`, and `modalHeadingEdit` are not in the UI-SPEC
table verbatim but are implied by its Interaction Notes ("Create Watch", "Edit Watch" headings, and
a management section). Add a comment marking them as documented spec extensions, matching how the
existing `emptyWatchesHeading` key already documents itself as an extension.

Do NOT modify or reword any existing key.
  </action>

  <verify>
    <automated>cd dashboard && npm run typecheck && node --input-type=module -e "import('./dashboard/lib/copy.ts')" 2>/dev/null; cd dashboard && npx tsx -e "import {COPY} from './lib/copy'; const need=['addWatch','saveWatch','discardChanges','toggleFacility','toggleArea','areaSearchPlaceholder','areaSearchNoResults','areaSearchMinChars','areaChipRemoveLabel','previewHeading','previewLoading','previewEmpty','previewTruncated','groupTag','unlockHeading','unlockBody','unlockPlaceholder','unlockSubmit','unlockError','saveFailed','areaSearchFailed','deleteConfirm','deleteConfirmYes','deleteConfirmNo','savedToast','deletedToast','editWatchLabel','deleteWatchLabel','modalHeadingCreate','modalHeadingEdit','sectionManageWatches']; const missing=need.filter(k=>!(k in COPY)); if(missing.length){console.error('MISSING',missing);process.exit(1)} console.log('all copy keys present')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -q "savedToast: 'Saved — live within ~5 min'" dashboard/lib/copy.ts` exits 0
    - `grep -q "deleteConfirmNo: 'Keep Watch'" dashboard/lib/copy.ts` exits 0
    - `grep -q "unlockHeading: 'Unlock to manage watches'" dashboard/lib/copy.ts` exits 0
    - `grep -q "addWatch: '+ Add Watch'" dashboard/lib/copy.ts` exits 0
    - `grep -q "groupTag: '\[GROUP\]'" dashboard/lib/copy.ts` exits 0
    - `grep -q "previewTruncated" dashboard/lib/copy.ts` exits 0 and the value contains `{kept}`, `{requested}`, and `{cap}`
    - `grep -q "pageTitle: 'Campground Crawler — Status'" dashboard/lib/copy.ts` exits 0 (existing keys untouched)
    - The tsx assertion in `<verify>` prints `all copy keys present`
    - `cd dashboard && npm run typecheck` exits 0
  </acceptance_criteria>

  <done>Every string plans 05-05 and 05-06 will render already exists as a `COPY` key with the exact UI-SPEC wording, so no component ever inlines a literal.</done>
</task>

<task type="auto">
  <name>Task 2: Add the phase's CSS classes to dashboard/app/globals.css using only existing tokens</name>

  <files>dashboard/app/globals.css</files>

  <read_first>
    - `dashboard/app/globals.css` — the file being modified; all 141 lines, so you see the existing token block and the exact formatting style of the existing class rules
    - `.planning/phases/05-watch-management-write-path/05-UI-SPEC.md` — Spacing Scale (incl. the 44px touch-target exception), Typography, Color, and Interaction Notes sections
  </read_first>

  <action>
Append a clearly-delimited Phase 5 block to `dashboard/app/globals.css`:

```css
/* --- Phase 5: watch management (05-UI-SPEC.md) ------------------------------
   No new custom properties. Every value below resolves to an existing --space-*,
   --text-*, --weight-*, or --color-* token, with two documented exceptions the
   UI-SPEC calls out explicitly: the 44px minimum touch target (this phase adds the
   dashboard's first clickable controls) and the toast's fixed 24px viewport offset.
   Adding a 5th font size, a 3rd weight, or any new hex value here is a spec violation. */
```

Define exactly these classes. Constraints that must hold for every rule:
- Color values are **only** `var(--color-*)`. No literal hex anywhere in the new block.
- Font sizes are **only** `var(--text-meta | --text-body | --text-heading | --text-title)`.
- Font weights are **only** `var(--weight-regular | --weight-semibold)`.
- Spacing is **only** `var(--space-*)`, except `min-height/min-width: 44px` and the toast's `24px` offset.

| Class | Purpose | Key declarations |
|-------|---------|------------------|
| `.manage-header` | Section header row holding the heading and the `+ Add Watch` CTA | `display: flex; justify-content: space-between; align-items: center; gap: var(--space-md);` |
| `.btn` | Base button | `font-family: var(--font-sans); font-size: var(--text-body); padding: var(--space-sm) var(--space-md); min-height: 44px; border-radius: 4px; border: 1px solid var(--color-border); background: var(--color-bg); color: var(--color-text); cursor: pointer;` |
| `.btn--primary` | The Save / Add / Unlock CTA — the **only** accent-colored buttons | `background: var(--color-accent); border-color: var(--color-accent); color: var(--color-bg); font-weight: var(--weight-semibold);` |
| `.btn--destructive` | Delete + delete-confirm | `background: var(--color-destructive); border-color: var(--color-destructive); color: var(--color-bg);` |
| `.btn--ghost` | Cancel / Discard / Keep Watch — neutral, never accent | `background: transparent; color: var(--color-text);` |
| `.btn--icon` | Per-row edit/delete icon buttons | `min-width: 44px; padding: var(--space-xs);` |
| `.btn:disabled` | — | `opacity: 0.5; cursor: not-allowed;` |
| `.toggle` | Facility/Area segmented control container | `display: inline-flex; border: 1px solid var(--color-border); border-radius: 4px; overflow: hidden;` |
| `.toggle-option` | One segment | inherits `.btn` sizing; `border: none; border-radius: 0;` |
| `.toggle-option--active` | Selected segment (accent per the Color contract) | `background: var(--color-accent); color: var(--color-bg); font-weight: var(--weight-semibold);` |
| `.field` | Label + input stack | `display: flex; flex-direction: column; gap: var(--space-sm);` |
| `.field-label` | — | `font-size: var(--text-meta); color: var(--color-meta);` |
| `.field-input` | Text/date/select input | `font-family: var(--font-sans); font-size: var(--text-body); padding: var(--space-sm); min-height: 44px; border: 1px solid var(--color-border); border-radius: 4px; background: var(--color-bg); color: var(--color-text);` |
| `.field-input:focus` | Focused-input accent border per the Color contract | `outline: none; border-color: var(--color-accent);` |
| `.field-error` | Inline validation error | `font-size: var(--text-meta); color: var(--color-destructive);` |
| `.form-sections` | Modal body vertical rhythm | `display: flex; flex-direction: column; gap: var(--space-xl);` |
| `.form-fields` | Within one section | `display: flex; flex-direction: column; gap: var(--space-md);` |
| `.chips` | Wrapped chip row | `display: flex; flex-wrap: wrap; gap: var(--space-sm); list-style: none; margin: 0; padding: 0;` |
| `.chip` | One area chip | `display: inline-flex; align-items: center; gap: var(--space-xs); padding: var(--space-xs) var(--space-sm); background: var(--color-surface); border: 1px solid var(--color-border); border-radius: 999px; font-size: var(--text-body);` |
| `.chip-remove` | The `×` control | `min-width: 44px; min-height: 44px; background: none; border: none; cursor: pointer; color: var(--color-meta); font-size: var(--text-body);` |
| `.typeahead` | Positioning context for the dropdown | `position: relative;` |
| `.typeahead-list` | Suggestion dropdown | `position: absolute; top: 100%; left: 0; right: 0; z-index: 10; margin: 0; padding: 0; list-style: none; background: var(--color-bg); border: 1px solid var(--color-border); border-radius: 4px; max-height: 240px; overflow-y: auto;` |
| `.typeahead-option` | One suggestion | `padding: var(--space-sm); font-size: var(--text-body); cursor: pointer;` |
| `.typeahead-option--active` | Keyboard-highlighted suggestion | `background: var(--color-surface);` |
| `.typeahead-hint` | Min-chars / no-results / error text | `font-size: var(--text-meta); color: var(--color-meta); padding: var(--space-sm);` |
| `.preview` | Preview panel | `background: var(--color-surface); padding: var(--space-lg); border-radius: 4px;` |
| `.preview-heading` | — | `font-size: var(--text-heading); font-weight: var(--weight-semibold); line-height: var(--leading-tight); color: var(--color-accent); margin: 0 0 var(--space-sm);` |
| `.preview-list` | — | `display: flex; flex-direction: column; gap: var(--space-sm); list-style: none; margin: 0; padding: 0; font-size: var(--text-body);` |
| `.preview-tag` | The `[GROUP]` tag — neutral gray, deliberately **not** a new semantic color | `color: var(--color-meta); font-size: var(--text-meta); margin-left: var(--space-xs);` |
| `.preview-truncated` | Cap warning | `font-size: var(--text-meta); color: var(--color-destructive); margin-top: var(--space-sm);` |
| `.dialog` | Native `<dialog>` for the create/edit modal | `border: 1px solid var(--color-border); border-radius: 8px; padding: var(--space-lg); background: var(--color-bg); color: var(--color-text); max-width: 640px; width: 100%;` |
| `.dialog--confirm` | The smaller delete-confirm dialog | `max-width: 420px;` |
| `.dialog::backdrop` | — | `background: rgba(17, 24, 39, 0.4);` (documented exception: `::backdrop` cannot use a token here; this is `--color-text` at 40% alpha, not a new palette entry — say so in a comment) |
| `.dialog-heading` | — | `font-size: var(--text-heading); font-weight: var(--weight-semibold); line-height: var(--leading-tight); color: var(--color-accent); margin: 0 0 var(--space-lg);` |
| `.dialog-actions` | Footer button row | `display: flex; justify-content: flex-end; gap: var(--space-sm); margin-top: var(--space-xl);` |
| `.unlock` | Inline unlock prompt block | `background: var(--color-surface); padding: var(--space-lg); border-radius: 4px; display: flex; flex-direction: column; gap: var(--space-sm);` |
| `.unlock-heading` | — | `font-size: var(--text-heading); font-weight: var(--weight-semibold); line-height: var(--leading-tight); color: var(--color-accent); margin: 0;` |
| `.unlock-body` | — | `font-size: var(--text-meta); color: var(--color-meta); margin: 0;` |
| `.unlock-form` | — | `display: flex; gap: var(--space-sm); align-items: flex-start;` |
| `.toast` | D-12 banner — non-blocking, fixed | `position: fixed; bottom: 24px; right: 24px; z-index: 100; background: var(--color-surface); color: var(--color-text); border: 1px solid var(--color-border); border-radius: 4px; padding: var(--space-lg); font-size: var(--text-meta);` — plus a comment: *not a modal; must not block interaction with the page underneath* |
  </action>

  <verify>
    <automated>cd dashboard && npm run build 2>&1 | tail -15 && python3 -c "
import re,sys
css=open('app/globals.css').read()
block=css.split('Phase 5: watch management')[1]
hexes=[h for h in re.findall(r'#[0-9A-Fa-f]{3,8}', block)]
if hexes: print('NEW HEX IN PHASE 5 BLOCK:', hexes); sys.exit(1)
bad=[s for s in re.findall(r'font-size:\s*([^;]+);', block) if 'var(--text-' not in s]
if bad: print('NON-TOKEN FONT SIZE:', bad); sys.exit(1)
bad=[s for s in re.findall(r'font-weight:\s*([^;]+);', block) if 'var(--weight-' not in s]
if bad: print('NON-TOKEN FONT WEIGHT:', bad); sys.exit(1)
print('globals.css phase 5 block uses tokens only')
"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -q "Phase 5: watch management" dashboard/app/globals.css` exits 0
    - `grep -q "^\.btn--primary" dashboard/app/globals.css` exits 0
    - `grep -q "^\.chip-remove" dashboard/app/globals.css` exits 0
    - `grep -q "^\.typeahead-list" dashboard/app/globals.css` exits 0
    - `grep -q "^\.preview-truncated" dashboard/app/globals.css` exits 0
    - `grep -q "^\.dialog" dashboard/app/globals.css` exits 0
    - `grep -q "^\.toast" dashboard/app/globals.css` exits 0
    - `grep -q "min-height: 44px" dashboard/app/globals.css` exits 0
    - The Python token check in `<verify>` prints `globals.css phase 5 block uses tokens only` and exits 0
    - `grep -c "\-\-space-4xl\|--text-small\|--weight-bold\|--color-warning" dashboard/app/globals.css` returns 0 (no invented tokens)
    - `cd dashboard && npm run build` exits 0
  </acceptance_criteria>

  <done>Every visual affordance the remaining UI plans need has a class, built entirely from Phase 3's token system — no new color, no fifth size, no third weight.</done>
</task>

<task type="auto">
  <name>Task 3: Create dashboard/app/watches/unlock-prompt.tsx — the inline passphrase form</name>

  <files>dashboard/app/watches/unlock-prompt.tsx</files>

  <read_first>
    - `dashboard/app/sections.tsx` — the presentational conventions to match: exclusive use of `COPY.*`, `aria-label` usage, class-name style. Note it is a **Server** Component with no `'use client'` — this new file is the codebase's first client component, so there is no behavioral analog
    - `dashboard/lib/copy.ts` — the `unlock*` keys added in Task 1
    - `.planning/phases/05-watch-management-write-path/05-UI-SPEC.md` — Interaction Notes bullet on the unlock prompt (D-02) and the Copywriting Contract rows for unlock
  </read_first>

  <action>
Create `dashboard/app/watches/unlock-prompt.tsx`:

```tsx
'use client';

/** Inline unlock form (D-02, MGMT-06).
 *
 *  Inline on the watches section, NOT a /login route: an unauthenticated visitor still sees the
 *  full read-only watch list (MGMT-01) — only the management controls are replaced by this form.
 *
 *  The session cookie is httpOnly and therefore unreadable from JavaScript. Lock state is passed
 *  down from the Server Component as a prop; this component never inspects document.cookie
 *  (which would always report "locked" and is also exactly the property that keeps the secret out
 *  of reach of injected script — threat T-05-17).
 */
import { useState } from 'react';
import { COPY } from '@/lib/copy';

export function UnlockPrompt({ onUnlocked }: { onUnlocked: () => void }) { /* ... */ }
```

Behavior:
1. `const [passphrase, setPassphrase] = useState('')`, `const [error, setError] = useState(false)`, `const [pending, setPending] = useState(false)`.
2. Render:
   ```tsx
   <div className="unlock">
     <h3 className="unlock-heading">{COPY.unlockHeading}</h3>
     <p className="unlock-body">{COPY.unlockBody}</p>
     <form className="unlock-form" onSubmit={handleSubmit}>
       <input
         className="field-input"
         type="password"
         name="passphrase"
         autoComplete="current-password"
         placeholder={COPY.unlockPlaceholder}
         aria-label={COPY.unlockPlaceholder}
         value={passphrase}
         onChange={(e) => { setPassphrase(e.target.value); setError(false); }}
       />
       <button className="btn btn--primary" type="submit" disabled={pending || passphrase.length === 0}>
         {COPY.unlockSubmit}
       </button>
     </form>
     {error ? <p className="field-error" role="alert">{COPY.unlockError}</p> : null}
   </div>
   ```
   `type="password"` is required — the passphrase must never be shoulder-surfable or land in a
   browser autofill history as plain text.
3. `handleSubmit`: `e.preventDefault()`, `setPending(true)`, then
   ```tsx
   const res = await fetch('/api/session', {
     method: 'POST',
     headers: { 'Content-Type': 'application/json' },
     body: JSON.stringify({ passphrase }),
   });
   setPending(false);
   if (res.ok) { setPassphrase(''); onUnlocked(); }
   else { setError(true); }
   ```
   Clear `passphrase` from state on success so the secret does not linger in a React tree that
   could end up in a devtools snapshot or an error report.
4. Wrap the `fetch` in try/catch; a network failure sets `error` (the generic
   `COPY.unlockError` copy) rather than throwing into an unhandled rejection.

`onUnlocked` is a callback, not a router refresh, so the parent (`watch-manager.tsx`, plan 05-05)
owns the unlocked state. Do not call `router.refresh()` here.

Do NOT create `watch-manager.tsx`, `watch-form.tsx`, `area-typeahead.tsx`, or `area-preview.tsx`
in this plan — plans 05-05 and 05-06 own those files.
  </action>

  <verify>
    <automated>cd dashboard && npm run typecheck && npm run build 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - `test -f dashboard/app/watches/unlock-prompt.tsx` exits 0
    - The first line of the file is `'use client';`
    - `grep -q "export function UnlockPrompt" dashboard/app/watches/unlock-prompt.tsx` exits 0
    - `grep -q "fetch('/api/session'" dashboard/app/watches/unlock-prompt.tsx` exits 0
    - `grep -q "type=\"password\"" dashboard/app/watches/unlock-prompt.tsx` exits 0
    - `grep -c "document.cookie" dashboard/app/watches/unlock-prompt.tsx` returns 0
    - `grep -q "onUnlocked" dashboard/app/watches/unlock-prompt.tsx` exits 0
    - `grep -c "COPY\." dashboard/app/watches/unlock-prompt.tsx` returns >= 5
    - No inline user-visible string literal in JSX: `grep -nE '>[A-Z][a-z]+ [a-z]' dashboard/app/watches/unlock-prompt.tsx` returns nothing
    - `cd dashboard && npm run typecheck` exits 0
    - `cd dashboard && npm run build` exits 0
  </acceptance_criteria>

  <done>An unauthenticated visitor can submit the shared passphrase inline on the watches section, sees a specific inline error on a wrong guess, and the parent is notified via `onUnlocked` — with the secret never touching `document.cookie` or a persistent client state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User keystrokes → `/api/session` | The shared secret transits the browser exactly once, in a POST body over HTTPS |
| Client component ↔ session cookie | Deliberately impassable: the cookie is `httpOnly` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-17 | Information Disclosure | passphrase in client state | mitigate | `type="password"` input; state cleared immediately on success; never written to `localStorage`, `sessionStorage`, or `document.cookie`; lock state arrives as a Server Component prop rather than being read from the cookie |
| T-05-18 | Information Disclosure | error message leaking secret shape | mitigate | A wrong passphrase renders the fixed `COPY.unlockError` string; the API's 401 body carries no detail to render, so nothing about length, prefix, or configuration state can surface |
| T-05-19 | Tampering | XSS via rendered strings | mitigate | Every string comes from the static `COPY` object; no `dangerouslySetInnerHTML` anywhere; the `{query}`/`{area}` placeholders are substituted into React text nodes, which React escapes |
| T-05-20 | Spoofing | offline passphrase brute force in the browser | mitigate | No secret or hash of it is ever sent to the client — validation is exclusively server-side in `/api/session`, so there is nothing local to attack |
</threat_model>

<verification>
1. `cd dashboard && npm run typecheck` — clean
2. `cd dashboard && npm run build` — exits 0
3. The Python token check on the `globals.css` Phase 5 block passes (no new hex, no non-token font size or weight)
4. The tsx COPY-key assertion prints `all copy keys present`
5. `grep -rn "localStorage\|sessionStorage\|document.cookie" dashboard/app/` returns nothing
</verification>

<success_criteria>
- All 31 new `COPY` keys exist with the exact UI-SPEC wording, and no existing key changed
- The Phase 5 `globals.css` block contains zero literal hex values, zero non-token font sizes, and zero non-token font weights
- `UnlockPrompt` renders the four unlock strings from `COPY`, POSTs to `/api/session`, shows the inline error on 401, and calls `onUnlocked()` on 200
- `npm run typecheck` and `npm run build` both pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-watch-management-write-path/05-04-SUMMARY.md`
</output>
